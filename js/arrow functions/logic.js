const hello = () => console.log("hyello");
hello();

const name = (nam) => console.log(`hey bom bom ${nam}`);
name("ananya"); // just a name don't get offended ananya! 😁

setTimeout(() => {
  console.log("hello");
}, 3000);
